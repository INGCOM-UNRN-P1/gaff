# 0x0007h: Los argumentos de función y las variables locales deben usar snake_case en minúsculas

```{admonition} Categoría: Sintaxis Básica y Nomenclatura (0x00XX)
:class: note
Código de regla: `0x0007h` | Soporta autofix automático: **No**
```

## Fundamentación y Justificación Pedagógica

Todos los identificadores de variables y argumentos deben estar escritos en snake_case en minúsculas, evitando camelCase o PascalCase.

El cumplimiento de esta pauta asegura la consistencia estilística requerida por la cátedra,
facilita la legibilidad para el equipo docente y minimiza errores sintácticos o semánticos en C.

## Ejemplos de Código

### Código No Conforme (Antipatrón)

```{code-block} c
int calcularPromedio(int *vectorNumeros, size_t LongitudTotal);
```

### Código Conforme (Recomendado)

```{code-block} c
int calcular_promedio(int *vector_numeros, size_t longitud_total);
```

## Instrucción de Corrección

Aplique las directivas sugeridas o ejecute `gaff fix` para aquellas reglas que dispongan de corrección automatizada.
