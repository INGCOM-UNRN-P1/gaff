# 0x0012h: Las variables globales deben ser declaradas como static o usar prefijo g_

```{admonition} Categoría: General
:class: note
Código de regla: `0x0012h` | Soporta autofix automático: **No**
```

## Fundamentación y Justificación Pedagógica

Las variables con alcance de archivo deben restringirse con static o usar explícitamente el prefijo g_ para visibilizar el acoplamiento global.

El cumplimiento de esta pauta asegura la consistencia estilística requerida por la cátedra,
facilita la legibilidad para el equipo docente y minimiza errores sintácticos o semánticos en C.

## Ejemplos de Código

### Código No Conforme (Antipatrón)

```{code-block} c
int total_acumulado = 0; // variable global no static
```

### Código Conforme (Recomendado)

```{code-block} c
static int g_contador_llamadas = 0;
```

## Instrucción de Corrección

Aplique las directivas sugeridas o ejecute `gaff fix` para aquellas reglas que dispongan de corrección automatizada.
