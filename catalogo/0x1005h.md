# 0x1005h: Evitá las condiciones ambiguas basadas en la "veracidad" (truthiness) del tipo de dato

```{admonition} Categoría: Estructuras de Control y Lazos (0x10XX)
:class: note
Código de regla: `0x1005h` | Soporta autofix automático: **No**
```

## Fundamentación y Justificación Pedagógica

Las comparaciones deben ser siempre explícitas. En C, cualquier valor numérico distinto de cero se considera verdadero, y el cero falso. Depender de esta veracidad implícita atenta contra la legibilidad. Es fundamental diferenciar de forma inequívoca la comparación de caracteres del chequeo de punteros: - Si la variable es un carácter (`char`), comparalo contra el carácter nulo de cadena `'\0'`. - Si la variable es un puntero, comparalo contra `NULL`. - Si es una variable lógica, comparala contra `true` o `false`.

El cumplimiento de esta pauta asegura la consistencia estilística requerida por la cátedra,
facilita la legibilidad para el equipo docente y minimiza errores sintácticos o semánticos en C.

## Ejemplos de Código

### Código No Conforme (Antipatrón)

```{code-block} c
if (x)
{
```

### Código Conforme (Recomendado)

```{code-block} c
if (x != 0)
{
```

## Instrucción de Corrección

Aplique las directivas sugeridas o ejecute `gaff fix` para aquellas reglas que dispongan de corrección automatizada.
