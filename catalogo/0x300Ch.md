# 0x300Ch: Verificá siempre los límites de los arreglos antes de acceder a sus elementos

```{admonition} Categoría: Punteros y Gestión de Memoria (0x30XX)
:class: note
Código de regla: `0x300Ch` | Soporta autofix automático: **No**
```

## Fundamentación y Justificación Pedagógica

El acceso fuera de límites de un arreglo (`out-of-bounds`) es un error grave. Los índices deben ser explícitamente validados antes de acceder a un elemento.

El cumplimiento de esta pauta asegura la consistencia estilística requerida por la cátedra,
facilita la legibilidad para el equipo docente y minimiza errores sintácticos o semánticos en C.

## Ejemplos de Código

### Código No Conforme (Antipatrón)

```{code-block} c
arr[indice] = x; // Sin chequear si indice < TAMANO
```

### Código Conforme (Recomendado)

```{code-block} c
if (indice >= 0 && indice < TAMANO) { arr[indice] = x; }
```

## Instrucción de Corrección

Aplique las directivas sugeridas o ejecute `gaff fix` para aquellas reglas que dispongan de corrección automatizada.
